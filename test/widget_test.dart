import 'package:flutter_test/flutter_test.dart';

import 'package:retroscape_modern/main.dart';

void main() {
  test('RetroScrapeApp can be constructed', () {
    const app = RetroScrapeApp();
    expect(app, isNotNull);
  });
}
